---
phase: 03-campus-spatial-hierarchy-location-apis
plan: "02"
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - frontend/src/types/campus.ts
  - frontend/src/api/client.ts
  - frontend/src/pages/Buildings.tsx
  - frontend/src/pages/BuildingDetail.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - LOC-02
  - LOC-03
user_setup: []
must_haves:
  truths:
    - "Users can browse all campus buildings with search and total floors metadata."
    - "Building detail view presents tabs for each floor showing classrooms, labs, offices, and rooms."
    - "Frontend integrates seamlessly with backend /api/buildings endpoints."
  artifacts:
    - frontend/src/types/campus.ts
    - frontend/src/pages/Buildings.tsx
    - frontend/src/pages/BuildingDetail.tsx
  key_links:
    - "Buildings page fetches list from campusApi in client.ts"
    - "BuildingDetail navigates floor-by-floor room inventories"
---

<objective>
Build frontend campus spatial browser views: Buildings directory page with instant search, and BuildingDetail page with floor selector and interactive room list.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-campus-spatial-hierarchy-location-apis/03-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: TypeScript Models & API Client</name>
  <files>frontend/src/types/campus.ts, frontend/src/api/client.ts</files>
  <action>
    - Define TypeScript interfaces: `Building`, `Floor`, `Room`, `RoomType`.
    - Extend `campusApi` in `client.ts` with `getBuildings(search?: string)`, `getBuilding(id: number)`, `getFloors(buildingId: number)`, `getRooms(floorId: number)`.
  </action>
  <verify>
    Run `npm run build` in frontend.
  </verify>
  <done>Frontend types and API functions created.</done>
</task>

<task type="auto">
  <name>Task 2: Buildings & BuildingDetail Pages</name>
  <files>frontend/src/pages/Buildings.tsx, frontend/src/pages/BuildingDetail.tsx, frontend/src/App.tsx</files>
  <action>
    - Create `Buildings.tsx` with search input, grid of building cards displaying name, code, address, coordinates badge, and "Explore Floors" action.
    - Create `BuildingDetail.tsx` displaying header info, floor pill tabs, and rooms grid grouped by type (Classroom, Lab, Faculty Office, etc.) with capacity badges.
    - Add routes `/buildings` and `/buildings/:id` in `App.tsx`.
  </action>
  <verify>
    Run `npm run build` in frontend to verify clean compilation.
  </verify>
  <done>Frontend campus exploration views complete.</done>
</task>

</tasks>

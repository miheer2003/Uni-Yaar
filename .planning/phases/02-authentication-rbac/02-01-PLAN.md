---
phase: 02-authentication-rbac
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/pom.xml
  - backend/src/main/resources/application.yml
  - backend/src/main/java/com/uniyar/entity/User.java
  - backend/src/main/java/com/uniyar/repository/UserRepository.java
  - backend/src/main/java/com/uniyar/service/UserService.java
  - backend/src/main/java/com/uniyar/security/JwtTokenProvider.java
  - backend/src/main/java/com/uniyar/security/JwtAuthenticationFilter.java
  - backend/src/main/java/com/uniyar/security/SecurityConfig.java
  - backend/src/main/java/com/uniyar/dto/auth/AuthRequest.java
  - backend/src/main/java/com/uniyar/dto/auth/AuthResponse.java
  - backend/src/main/java/com/uniyar/controller/AuthController.java
autonomous: true
requirements:
  - AUTH-01
  - AUTH-02
  - AUTH-03
user_setup: []
must_haves:
  truths:
    - "User registration with BCrypt password hashing works via POST /api/auth/register"
    - "User login with JWT token issuance works via POST /api/auth/login"
    - "Spring Security filter chain protects endpoints based on roles (ROLE_STUDENT, ROLE_FACULTY, ROLE_STAFF, ROLE_ADMIN)"
    - "Password is never returned in API responses"
    - "JWT tokens include role claims for authorization"
  artifacts:
    - backend/src/main/java/com/uniyar/entity/User.java
    - backend/src/main/java/com/uniyar/repository/UserRepository.java
    - backend/src/main/java/com/uniyar/service/UserService.java
    - backend/src/main/java/com/uniyar/security/JwtTokenProvider.java
    - backend/src/main/java/com/uniyar/security/JwtAuthenticationFilter.java
    - backend/src/main/java/com/uniyar/security/SecurityConfig.java
    - backend/src/main/java/com/uniyar/controller/AuthController.java
  key_links:
    - "JwtTokenProvider generates and validates JWT tokens"
    - "JwtAuthenticationFilter extracts and validates JWT from Authorization header"
    - "SecurityConfig configures filter chain with role-based access rules"
    - "AuthController handles register/login endpoints"
    - "UserService handles user creation and lookup"
---
<objective>
Implement backend authentication with JWT tokens, BCrypt password encryption, and Spring Security role-based access control. Create User entity, registration/login endpoints, JWT token provider, authentication filter, and security configuration with 4 roles (STUDENT, FACULTY, STAFF, ADMIN).
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-architecture-monorepo-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Security and JWT Dependencies to POM</name>
  <files>backend/pom.xml</files>
  <action>
    Add spring-boot-starter-security, jjwt-api, jjwt-impl, jjwt-jackson dependencies to pom.xml. These provide Spring Security, JWT token generation/validation.
  </action>
  <verify>
    Run ./mvnw compile -DskipTests to verify dependencies resolve correctly.
  </verify>
  <acceptance_criteria>
    - backend/pom.xml contains spring-boot-starter-security
    - backend/pom.xml contains jjwt-api, jjwt-impl, jjwt-jackson
  </acceptance_criteria>
  <done>Security and JWT dependencies added to Maven project.</done>
</task>

<task type="auto">
  <name>Task 2: Create User Entity with Role Enum</name>
  <files>backend/src/main/java/com/uniyar/entity/User.java</files>
  <action>
    Create User JPA entity with fields: id (Long), email (String, unique), password (String), fullName (String), role (UserRole enum: STUDENT, FACULTY, STAFF, ADMIN), createdAt, updatedAt. Add @Entity, @Table(name="users"), indexes on email. Use Lombok annotations. Role enum should have Spring Security role prefix (ROLE_STUDENT, etc.)
  </action>
  <verify>
    Verify file compiles with Maven.
  </verify>
  <acceptance_criteria>
    - User entity has all required fields with proper JPA annotations
    - UserRole enum exists with 4 roles
    - Email is unique indexed
  </acceptance_criteria>
  <done>User entity and role enum created.</done>
</task>

<task type="auto">
  <name>Task 3: Create User Repository</name>
  <files>backend/src/main/java/com/uniyar/repository/UserRepository.java</files>
  <action>
    Create UserRepository extending JpaRepository<User, Long> with findByEmail(String email) method returning Optional<User>.
  </action>
  <verify>
    Verify file compiles with Maven.
  </verify>
  <acceptance_criteria>
    - UserRepository extends JpaRepository
    - findByEmail method exists
  </acceptance_criteria>
  <done>User repository created.</done>
</task>

<task type="auto">
  <name>Task 4: Create Auth DTOs (Request/Response)</name>
  <files>backend/src/main/java/com/uniyar/dto/auth/AuthRequest.java, backend/src/main/java/com/uniyar/dto/auth/AuthResponse.java</files>
  <action>
    Create AuthRequest with email, password, fullName (for register), role (optional, default STUDENT). Add Bean Validation @NotBlank, @Email, @Size. Create AuthResponse with token (String), userId (Long), email (String), fullName (String), role (String). Both use Lombok @Data.
  </action>
  <verify>
    Verify files compile with Maven.
  </verify>
  <acceptance_criteria>
    - AuthRequest has email, password, fullName, role with validation
    - AuthResponse has token, userId, email, fullName, role
  </acceptance_criteria>
  <done>Auth DTOs created.</done>
</task>

<task type="auto">
  <name>Task 5: Create JwtTokenProvider</name>
  <files>backend/src/main/java/com/uniyar/security/JwtTokenProvider.java</files>
  <action>
    Create JwtTokenProvider component with methods: generateToken(User user) - creates JWT with subject=email, claims for userId, role, expiration (24h); validateToken(String token) - returns boolean; getEmailFromToken(String token) - extracts email; getRoleFromToken(String token) - extracts role. Use @Value for secret key and expiration. Use jjwt library.
  </action>
  <verify>
    Verify file compiles with Maven.
  </verify>
  <acceptance_criteria>
    - generateToken creates valid JWT with claims
    - validateToken returns true for valid tokens
    - getEmailFromToken extracts email correctly
  </acceptance_criteria>
  <done>JWT token provider implemented.</done>
</task>

<task type="auto">
  <name>Task 6: Create JwtAuthenticationFilter</name>
  <files>backend/src/main/java/com/uniyar/security/JwtAuthenticationFilter.java</files>
  <action>
    Create JwtAuthenticationFilter extending OncePerRequestFilter. Override doFilterInternal to extract Bearer token from Authorization header, validate using JwtTokenProvider, set authentication in SecurityContext if valid. Add @Component and @RequiredArgsConstructor.
  </action>
  <verify>
    Verify file compiles with Maven.
  </verify>
  <acceptance_criteria>
    - Filter extracts Bearer token from header
    - Validates token using JwtTokenProvider
    - Sets authentication in SecurityContext
  </acceptance_criteria>
  <done>JWT authentication filter created.</done>
</task>

<task type="auto">
  <name>Task 7: Create SecurityConfig</name>
  <files>backend/src/main/java/com/uniyar/security/SecurityConfig.java</files>
  <action>
    Create SecurityConfig with @EnableWebSecurity and @EnableMethodSecurity. Configure SecurityFilterChain bean: disable CSRF, set session management to STATELESS, permit all access to /api/auth/**, /api/health, /api/, require authentication for /api/admin/** (ADMIN), /api/staff/** (STAFF, ADMIN), /api/faculty/** (FACULTY, STAFF, ADMIN). Add JwtAuthenticationFilter before UsernamePasswordAuthenticationFilter. Configure PasswordEncoder bean using BCryptPasswordEncoder.
  </action>
  <verify>
    Verify file compiles with Maven.
  </verify>
  <acceptance_criteria>
    - SecurityFilterChain configured with stateless sessions
    - Public endpoints: /api/auth/**, /api/health, /api/
    - Protected endpoints by role
    - BCryptPasswordEncoder bean configured
    - JwtAuthenticationFilter registered
  </acceptance_criteria>
  <done>Security configuration completed.</done>
</task>

<task type="auto">
  <name>Task 8: Create UserService</name>
  <files>backend/src/main/java/com/uniyar/service/UserService.java</files>
  <action>
    Create UserService with @Service and @RequiredArgsConstructor. Methods: register(AuthRequest request) - checks email uniqueness, hashes password with BCrypt, saves user with default STUDENT role, returns User; findByEmail(String email) - returns Optional<User>; getUserById(Long id) - returns User or throws ResourceNotFoundException. Inject UserRepository and PasswordEncoder.
  </action>
  <verify>
    Verify file compiles with Maven.
  </verify>
  <acceptance_criteria>
    - register method hashes password with BCrypt
    - Email uniqueness check implemented
    - findByEmail and getUserById methods exist
  </acceptance_criteria>
  <done>User service implemented.</done>
</task>

<task type="auto">
  <name>Task 9: Create AuthController</name>
  <files>backend/src/main/java/com/uniyar/controller/AuthController.java</files>
  <action>
    Create AuthController with @RestController @RequestMapping("/api/auth"). Endpoints: POST /register - takes @Valid AuthRequest, calls UserService.register, generates JWT via JwtTokenProvider, returns AuthResponse; POST /login - takes @Valid AuthRequest (email, password), validates credentials via UserService, generates JWT, returns AuthResponse. Handle errors with proper HTTP status codes.
  </action>
  <verify>
    Run ./mvnw test to verify compilation and tests pass.
  </verify>
  <acceptance_criteria>
    - POST /api/auth/register creates user and returns JWT
    - POST /api/auth/login validates credentials and returns JWT
    - Password is never returned in response
  </acceptance_criteria>
  <done>Auth controller with register and login endpoints created.</done>
</task>

</tasks>

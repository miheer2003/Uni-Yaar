---
phase: 02-authentication-rbac
plan: "02"
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - frontend/src/api/client.ts
  - frontend/src/context/AuthContext.tsx
  - frontend/src/hooks/useAuth.ts
  - frontend/src/pages/Login.tsx
  - frontend/src/pages/Register.tsx
  - frontend/src/components/layout/Navbar.tsx
  - frontend/src/types/auth.ts
autonomous: true
requirements:
  - AUTH-04
user_setup: []
must_haves:
  truths:
    - "Frontend Axios client automatically attaches JWT token from localStorage"
    - "AuthContext provides user state, login, register, logout functions"
    - "Login and Register pages with form validation work end-to-end"
    - "Navbar updates to show user avatar/menu when authenticated"
    - "Protected routes redirect to login when unauthenticated"
  artifacts:
    - frontend/src/api/client.ts
    - frontend/src/context/AuthContext.tsx
    - frontend/src/pages/Login.tsx
    - frontend/src/pages/Register.tsx
  key_links:
    - "AuthContext uses api/client.ts for auth API calls"
    - "Navbar consumes AuthContext for user display"
    - "ProtectedRoute wrapper checks auth state"
---
<objective>
Implement frontend authentication: Axios interceptor for JWT, React Context for auth state, Login/Register pages with validation, and Navbar integration showing user session.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-authentication-rbac/02-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update API Client with Auth Interceptors</name>
  <files>frontend/src/api/client.ts</files>
  <action>
    Update axios client to: add request interceptor that attaches Authorization: Bearer <token> from localStorage; add response interceptor that on 401 clears localStorage and redirects to /login; export login, register, getProfile API functions.
  </action>
  <verify>
    Run npm run build in frontend to verify TypeScript compilation.
  </verify>
  <acceptance_criteria>
    - Request interceptor adds Bearer token
    - Response interceptor handles 401 by clearing auth
    - login, register, getProfile functions exported
  </acceptance_criteria>
  <done>API client updated with auth interceptors.</done>
</task>

<task type="auto">
  <name>Task 2: Create Auth Types</name>
  <files>frontend/src/types/auth.ts</files>
  <action>
    Create TypeScript interfaces: User { id, email, fullName, role, createdAt }, AuthResponse { token, userId, email, fullName, role }, LoginRequest { email, password }, RegisterRequest { email, password, fullName, role }.
  </action>
  <verify>
    Run npm run build in frontend to verify TypeScript compilation.
  </verify>
  <acceptance_criteria>
    - User, AuthResponse, LoginRequest, RegisterRequest interfaces defined
  </acceptance_criteria>
  <done>Auth types created.</done>
</task>

<task type="auto">
  <name>Task 3: Create AuthContext</name>
  <files>frontend/src/context/AuthContext.tsx</files>
  <action>
    Create AuthContext with React.createContext. Provider component manages: user (User | null), loading (boolean), login(credentials), register(data), logout(), checkAuth() - calls getProfile on mount. Value exposes user, loading, login, register, logout, isAuthenticated. Wrap App in AuthProvider.
  </action>
  <verify>
    Run npm run build in frontend to verify TypeScript compilation.
  </verify>
  <acceptance_criteria>
    - AuthContext provides user, loading, login, register, logout, isAuthenticated
    - checkAuth calls getProfile on mount
    - AuthProvider wraps application
  </acceptance_criteria>
  <done>Auth context created and integrated.</done>
</task>

<task type="auto">
  <name>Task 4: Create Login Page</name>
  <files>frontend/src/pages/Login.tsx</files>
  <action>
    Create Login page with email/password form, validation, error display, loading state. Use react-hook-form for validation. On submit call auth.login(), on success redirect to previous page or home. Link to Register page. Modern UniYaar styling with Tailwind.
  </action>
  <verify>
    Run npm run build in frontend to verify TypeScript compilation.
  </verify>
  <acceptance_criteria>
    - Form with email, password fields
    - Validation with react-hook-form
    - Error handling and loading state
    - Redirect on success
  </acceptance_criteria>
  <done>Login page created.</done>
</task>

<task type="auto">
  <name>Task 5: Create Register Page</name>
  <files>frontend/src/pages/Register.tsx</files>
  <action>
    Create Register page with fullName, email, password, confirmPassword, role select (Student/Faculty/Staff/Admin). Validation for matching passwords, email format. On submit call auth.register(), on success redirect to home. Link to Login page. Modern UniYaar styling.
  </action>
  <verify>
    Run npm run build in frontend to verify TypeScript compilation.
  </verify>
  <acceptance_criteria>
    - Form with fullName, email, password, confirmPassword, role
    - Password match validation
    - Role selection dropdown
    - Redirect on success
  </acceptance_criteria>
  <done>Register page created.</done>
</task>

<task type="auto">
  <name>Task 6: Update Navbar with Auth Integration</name>
  <files>frontend/src/components/layout/Navbar.tsx</files>
  <action>
    Update Navbar to consume AuthContext. Show user avatar/name dropdown when authenticated with links to Profile, Settings, Logout. Show Sign In / Sign Up buttons when not authenticated. Dropdown includes role badge. Logout calls auth.logout().
  </action>
  <verify>
    Run npm run build in frontend to verify TypeScript compilation.
  </verify>
  <acceptance_criteria>
    - Shows user dropdown when authenticated
    - Shows Sign In/Up when not authenticated
    - Logout button calls auth.logout()
    - Role badge displayed
  </acceptance_criteria>
  <done>Navbar updated with auth integration.</done>
</task>

<task type="auto">
  <name>Task 7: Add ProtectedRoute Component and Routes</name>
  <files>frontend/src/components/auth/ProtectedRoute.tsx, frontend/src/App.tsx</files>
  <action>
    Create ProtectedRoute wrapper that checks AuthContext.isAuthenticated, redirects to /login with return URL if not. Update App.tsx to add /login, /register routes and wrap protected routes with ProtectedRoute.
  </action>
  <verify>
    Run npm run build in frontend to verify TypeScript compilation.
  </verify>
  <acceptance_criteria>
    - ProtectedRoute redirects unauthenticated users to /login
    - /login and /register routes added
    - Protected routes wrapped
  </acceptance_criteria>
  <done>Protected routes and auth pages integrated.</done>
</task>

</tasks>

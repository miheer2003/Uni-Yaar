---
phase: 06-food-mess-module
plan: "02"
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - frontend/src/types/food.ts
  - frontend/src/api/client.ts
  - frontend/src/pages/FoodMess.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - FOOD-01
  - FOOD-02
  - FOOD-03
  - FOOD-04
user_setup: []
must_haves:
  truths:
    - "Students can view campus mess and canteen outlets with opening hours and open/closed status."
    - "Food page provides meal tabs: Breakfast, Lunch, Snacks, and Dinner."
    - "Dietary filter pills (All, Pure Veg, Jain, Non-Veg) filter menu items."
    - "Date selector toggles between Today and Tomorrow's menu."
    - "Direct 'View on Map' button opens the canteen's location on the campus map."
  artifacts:
    - frontend/src/types/food.ts
    - frontend/src/pages/FoodMess.tsx
  key_links:
    - "FoodMess fetches facilities and menus from foodApi in client.ts"
    - "FoodMess allows navigating to campus map for each outlet"
---

<objective>
Build the frontend 'Khane Ka Kya Scene Hai?' Food & Mess page with meal category tabs, dietary filter pills, date selector, and food outlet cards with map navigation.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-food-mess-module/06-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Food TypeScript Interfaces and API Methods</name>
  <files>frontend/src/types/food.ts, frontend/src/api/client.ts</files>
  <action>
    - Define TypeScript models: `FoodFacility`, `Menu`, `MenuItem`, `MealType`, `DietaryTag`.
    - Extend `foodApi` in `client.ts` with `getFacilities()` and `getMenu(facilityId: number, date?: string)`.
  </action>
  <verify>
    Run `npm run build` in frontend.
  </verify>
  <done>Food types and API client functions created.</done>
</task>

<task type="auto">
  <name>Task 2: Food & Mess UI Page ('Khane Ka Kya Scene Hai?')</name>
  <files>frontend/src/pages/FoodMess.tsx, frontend/src/App.tsx</files>
  <action>
    - Build `FoodMess.tsx` with:
      - Header "Khane Ka Kya Scene Hai?" with university mess timings.
      - Outlets selector (Central Mess, Tech Park Canteen, Cafe Coffee Hub).
      - Date switcher (Today / Tomorrow).
      - Meal tabs: Breakfast, Lunch, Snacks, Dinner.
      - Dietary filter pills: All, Pure Veg (green badge), Jain Available (yellow badge), Non-Veg (red badge).
      - Items menu cards with dietary dots and descriptions.
      - "View Canteen on Map" action link.
    - Wire `/food` route in `App.tsx`.
  </action>
  <verify>
    Run `npm run build` in frontend to verify clean compilation.
  </verify>
  <done>Frontend food and mess page ready and verified.</done>
</task>

</tasks>

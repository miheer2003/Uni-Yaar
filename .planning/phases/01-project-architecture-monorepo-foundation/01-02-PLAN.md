---
phase: 01-project-architecture-monorepo-foundation
plan: "02"
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/src/index.css
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/api/client.ts
  - frontend/src/components/layout/Navbar.tsx
  - frontend/src/components/layout/Footer.tsx
autonomous: true
requirements:
  - ARCH-02
user_setup: []
must_haves:
  truths:
    - "Frontend builds cleanly with Vite, React 18/19, and TypeScript."
    - "Tailwind CSS utility classes and custom color palette are working."
    - "Axios API client is configured with baseUrl pointing to backend (/api) with error interception."
    - "Core application layout shell (Navbar, Main container, Footer) renders with modern UniYaar startup branding."
  artifacts:
    - frontend/package.json
    - frontend/vite.config.ts
    - frontend/src/index.css
    - frontend/src/App.tsx
    - frontend/src/api/client.ts
    - frontend/src/components/layout/Navbar.tsx
  key_links:
    - "Axios client configures standardized API calls to backend"
    - "App.tsx renders responsive layout shell with UniYaar theme"
---

<objective>
Initialize the UniYaar frontend web application using Vite, React, TypeScript, Tailwind CSS, Lucide / MUI icons, Framer Motion, and Axios. Establish the brand design system tokens, responsive navbar with quick links, footer, and basic routing shell.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-project-architecture-monorepo-foundation/01-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bootstrap Vite React TypeScript Project with Dependencies</name>
  <files>frontend/package.json, frontend/vite.config.ts, frontend/tsconfig.json, frontend/tailwind.config.js, frontend/postcss.config.js</files>
  <action>
    Initialize frontend project:
    - Use `npm create vite@latest frontend -- --template react-ts`
    - Install dependencies: `tailwindcss postcss autoprefixer lucide-react framer-motion react-router-dom axios leaflet react-leaflet @types/leaflet clsx tailwind-merge`
    - Initialize Tailwind CSS with custom UniYaar theme colors:
      - Primary brand: vibrant saffron/amber accent (`#f59e0b` / `#ea580c`), deep slate/indigo background (`#0f172a`), clean card surfaces (`#1e293b`), and emerald accents for availability (`#10b981`).
    - Setup Vite proxy in `vite.config.ts` forwarding `/api` to `http://localhost:8080`.
  </action>
  <verify>
    Run `npm run build` inside `frontend/` to confirm error-free TypeScript compilation.
  </verify>
  <acceptance_criteria>
    - frontend/package.json contains react, vite, tailwindcss, react-router-dom, axios, leaflet
    - frontend/vite.config.ts configures proxy for /api
  </acceptance_criteria>
  <done>Frontend project configured with all modern UI and routing dependencies.</done>
</task>

<task type="auto">
  <name>Task 2: Build Brand Design System, API Client, and Layout Scaffolding</name>
  <files>frontend/src/index.css, frontend/src/api/client.ts, frontend/src/components/layout/Navbar.tsx, frontend/src/components/layout/Footer.tsx, frontend/src/App.tsx</files>
  <action>
    - Configure `frontend/src/index.css` with Google Font (Inter / Outfit / Plus Jakarta Sans) and custom design tokens.
    - Create `frontend/src/api/client.ts` setting up Axios instance with base URL, timeout, and response interceptor for standard `ApiResponse<T>`.
    - Create responsive `Navbar.tsx` featuring:
      - UniYaar logo with tagline "Apni Uni. Apna Yaar."
      - Quick links (Campus Map, Faculty, Food & Mess, Events, Facilities)
      - Search trigger button ("Search your campus... ⌘K")
      - Role/Profile & Sign In buttons
    - Create `Footer.tsx` with campus quick links, emergency info note, and brand credits.
    - Set up `App.tsx` with React Router and a vibrant Welcome Hero section showcasing the live backend connection status via `client.ts` calling `/api/health`.
  </action>
  <verify>
    Build frontend bundle with `npm run build` in `frontend/` directory.
  </verify>
  <acceptance_criteria>
    - frontend/src/index.css imports modern font and tailwind directives
    - frontend/src/api/client.ts handles API communications
    - frontend/src/App.tsx renders responsive layout with live health check indicator
  </acceptance_criteria>
  <done>Frontend layout and design foundation ready and verified.</done>
</task>

</tasks>

---
phase: 04-interactive-campus-map-navigation-engine
plan: "02"
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/src/types/map.ts
  - frontend/src/api/client.ts
  - frontend/src/pages/CampusMap.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - MAP-01
  - MAP-02
  - MAP-03
  - MAP-04
user_setup: []
must_haves:
  truths:
    - "Interactive Leaflet + OpenStreetMap map renders campus tile layer centered on coordinates."
    - "Category filter bar toggles marker categories (All, Buildings, Food, Labs, Facilities)."
    - "Selecting any marker opens an interactive slide-in details drawer with description, floors, and 'Start Walking Route' action."
    - "Walking navigation mode calculates and draws simulated path between student origin and target location with distance and estimated time."
  artifacts:
    - frontend/src/types/map.ts
    - frontend/src/pages/CampusMap.tsx
  key_links:
    - "CampusMap fetches markers from mapApi in client.ts"
    - "CampusMap uses Leaflet / react-leaflet to render map and polylines"
---

<objective>
Implement the interactive Leaflet + OpenStreetMap campus map page with custom marker layer, category filter pills, location details drawer, and simulated walking route navigation.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-interactive-campus-map-navigation-engine/04-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Leaflet Types & API Client Integration</name>
  <files>frontend/src/types/map.ts, frontend/src/api/client.ts</files>
  <action>
    - Define TypeScript models for `MapMarker`, `MapCategory`, `RouteData`.
    - Add `mapApi.getMarkers(category?: string)` to `client.ts`.
  </action>
  <verify>
    Run `npm run build` in frontend.
  </verify>
  <done>Frontend map types and API ready.</done>
</task>

<task type="auto">
  <name>Task 2: Interactive CampusMap Component & Walking Route Engine</name>
  <files>frontend/src/pages/CampusMap.tsx, frontend/src/App.tsx</files>
  <action>
    - Create `CampusMap.tsx` using `react-leaflet` with `MapContainer`, `TileLayer`, `Marker`, `Popup`, `Polyline`.
    - Fix Leaflet marker icon asset issue standard in Vite bundles (configure SVG/custom div icons).
    - Add category filters: All, Buildings, Food & Mess, Labs, Washrooms, Facilities.
    - Add location details drawer sliding in from the right or bottom when a pin is selected.
    - Implement simulated walking route calculation: draws a glowing path from starting gate or user location to the destination pin with distance (in meters) and walking time (in minutes).
    - Connect `/map` route in `App.tsx` replacing the placeholder.
  </action>
  <verify>
    Run `npm run build` in frontend to verify clean compilation.
  </verify>
  <done>Interactive campus map with navigation ready and verified.</done>
</task>

</tasks>

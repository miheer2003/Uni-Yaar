---
phase: 05-faculty-finder-scheduled-timetables
plan: "02"
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - frontend/src/types/faculty.ts
  - frontend/src/api/client.ts
  - frontend/src/pages/FacultyFinder.tsx
  - frontend/src/pages/FacultyDetail.tsx
  - frontend/src/App.tsx
autonomous: true
requirements:
  - FAC-01
  - FAC-02
  - FAC-03
  - FAC-04
user_setup: []
must_haves:
  truths:
    - "Students can search faculty by professor name, department, or subject."
    - "Faculty card displays designation, department, office room, and email contact."
    - "Faculty profile presents weekly timetable clearly designated as 'Scheduled Location'."
    - "Direct 'View on Map' action leads students to the professor's office or scheduled room."
  artifacts:
    - frontend/src/types/faculty.ts
    - frontend/src/pages/FacultyFinder.tsx
    - frontend/src/pages/FacultyDetail.tsx
  key_links:
    - "FacultyFinder fetches data from facultyApi in client.ts"
    - "FacultyDetail displays schedule with link to campus map"
---

<objective>
Implement frontend Faculty Finder directory with instant search and department filtering, and FacultyDetail profile view showing weekly scheduled timetables and map actions.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-faculty-finder-scheduled-timetables/05-01-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Faculty Types and API Client Methods</name>
  <files>frontend/src/types/faculty.ts, frontend/src/api/client.ts</files>
  <action>
    - Define TypeScript models for `Faculty` and `TimetableEntry`.
    - Extend `facultyApi` in `client.ts` with `getFacultyList(search?: string, departmentId?: number)`, `getFaculty(id: number)`, and `getTimetable(id: number)`.
  </action>
  <verify>
    Run `npm run build` in frontend.
  </verify>
  <done>Faculty types and client methods created.</done>
</task>

<task type="auto">
  <name>Task 2: Faculty Finder & Detail Profile Pages</name>
  <files>frontend/src/pages/FacultyFinder.tsx, frontend/src/pages/FacultyDetail.tsx, frontend/src/App.tsx</files>
  <action>
    - Build `FacultyFinder.tsx`: Search bar ("Search by name, department, or subject..."), filter tags, cards with faculty avatar, designation, office location badge, and subjects.
    - Build `FacultyDetail.tsx`: Faculty header with contact info, office room link, and weekly timetable grid (Monday–Friday) showing scheduled times, subjects, and rooms with explicit "Scheduled Location" badge.
    - Wire `/faculty` and `/faculty/:id` routes in `App.tsx`.
  </action>
  <verify>
    Run `npm run build` in frontend.
  </verify>
  <done>Frontend Faculty Finder views built and verified.</done>
</task>

</tasks>
